{
  "name": "nirbhaya-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NIRBHAYA incident registry on IPFS + Blockchain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy_incident_registry.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy_local.js --network localhost",
    "verify": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
